# codegraph serve

Start the local Cytoscape visualizer.

```bash
codegraph serve [--port 8080] [--backend sqlite|dgraph] [--no-open-browser]
```

Time slider, filters, and decision cards. Keyboard: R / F / Esc.
