# codegraph export

Write an AI-agent context pack.

```bash
codegraph export --for-ai --scope src [--output .codegraph/context.md]
```

Paste the output into your coding assistant before editing those files.
