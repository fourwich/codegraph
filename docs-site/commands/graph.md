# codegraph graph

Historical code-graph snapshot (time travel).

```bash
codegraph graph --at YYYY-MM-DD [--scope path] [--backend sqlite|dgraph]
```

Counts nodes **and** edges whose endpoints are valid at that date.
