# Coding assistants

Point your coding assistant at the context pack before editing those files.

```bash
codegraph export --for-ai --scope src
```

Paste `.codegraph/context.md` into the chat (or attach it as project context).
