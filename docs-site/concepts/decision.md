# Decision

A design choice with provenance.

Fields: content, reason, alternatives, status, source, source_ref, timestamp, author, constraints, confidence.

Extracted from commits (keywords + conventional subjects), ADR markdown, and CHANGELOG sections. Optional LLM fallback via Ollama.
